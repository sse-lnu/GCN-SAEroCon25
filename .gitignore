# Python bytecode
__pycache__/
*.pyc
*.pyo

# Notebooks — dev/exploration only, not part of the published CLI pipeline
*.ipynb
.ipynb_checkpoints/

# Experiment outputs — large, reproducible from code
results/
cache/

# Dev/scratch scripts not part of the published pipeline
smoke_test.py
time_calculation.py

# Large datasets — not included in repo
data/chromium*.csv
data/libxml*.csv

# Analysis and legacy code carried over from prior work — not part of the
# published pipeline
analysis/
legacy/
